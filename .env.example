# HyprDash Daemon Configuration

# Daemon port
PORT=8080

# Panel connection settings
PANEL_URL=http://localhost:3000
NODE_TOKEN=your-node-token-from-panel

# Servers data directory
SERVERS_PATH=/var/lib/hyprdash/servers

# Backups directory
BACKUPS_PATH=/var/lib/hyprdash/backups

# Optional: Docker socket path
# DOCKER_SOCKET=/var/run/docker.sock
